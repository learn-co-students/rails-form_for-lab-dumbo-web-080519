<h1>Students list</h1>

<%= link_to "Make a new student", new_student_path %>

<ul>
  <% @students.each do |student| %>
    <li>
      <%= link_to student.to_s, student %>
    </li>
  <% end %>
</ul>
